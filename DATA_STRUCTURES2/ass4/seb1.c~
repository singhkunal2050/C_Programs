/*REVERSE A STRING USING */

#include<stdio.h>
#include<string.h>
#include"setb1.h"

int main()
{

int i,n;
char str[20];
struct stack stk;
initstack(&stk);

printf("Enter a String to Reverse it!!\n");
scanf("%s",str);

printf("\n+_+\n");
while(str[i]!='\0')
	push(&stk,str[i++]);
/*
i=0;
//while(!isEmpty(&stk))
{
	str[i++]=pop(&stk);
}

printf("Your String in Reverse!!\n");
puts(str);
*/		


}
